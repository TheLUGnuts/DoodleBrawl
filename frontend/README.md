# Doodle Brawler Frontend

Designed with Vite + React, using Chakra UI as a frontend.

# Running

* `npm install` to install required components

* `npm run dev` to open a hot-reloading dev server

* `npm run build` to build a production-ready view.
